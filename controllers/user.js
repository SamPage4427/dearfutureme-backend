const User = require("../models/user");

const getUser = (req, res, next) => {};

const createUser = (req, res, next) => {};

const signinUser = (req, res, next) => {};

module.exports = { getUser, createUser, signinUser };
